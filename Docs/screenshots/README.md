# Screenshots — STALE, do not submit

These captures are from **2026-08-26** and no longer match the build. Do not use
them for Devpost, the Design Intent document, or any judged surface.

Known mismatches:

- `4_gameplay.png` shows the pick checklist as `Wasser (Water)` — **German first**.
  The build has been **English first** (`Milk (die Milch)`) since; see
  `src/ui/hud.js`. Submitting this actively contradicts the English-first pillar
  that every submission document leads with.
- `2_gameplay.png` shows only the €20 / €250 bar. The current HUD also shows the
  `DAY 1/28` counter and the 4-document `DOCS` tracker.
- All of them predate the Kenney food models rendering on the shelf, which only
  started working on 2026-09-02.

**Reshoot these in the same session as the gameplay video** so the stills and the
video agree with each other and with the build. See [`../TASKS.md`](../TASKS.md).
