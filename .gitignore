# Operating System & Editor Files
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Environment & Dependencies
node_modules/
.env
.env.local

# Executables, Binaries & Temporary Network/Deploy Artifacts
*.exe
*.zip
tunnel_url.txt
.deploy/
.claude/
.gemini/

# Working Draft Reference Media (User Request)
Docs/*.jpg
Docs/*.jpeg
Docs/*.png
Docs/*.html
Docs/archive/*.html

# Note: ONE_PAGE_DESIGN_DOCUMENT.md is deliberately NOT ignored. It is an
# authoritative design document (see Docs/README.md) and must be versioned.
# THE_IMMIGRANT_ODYSSEY_COMPLETE_STORY.md now lives in Docs/archive/ and is
# tracked there for history.

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
